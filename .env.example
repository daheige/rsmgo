# Optional: this file is only a convenience for setting shell environment variables
# that are referenced by app.yaml via ${VAR} placeholders. app.yaml is the single
# source of runtime configuration; you can also export variables directly in your
# shell or put values directly into app.yaml.
# Only the key for the provider you enable in app.yaml is required; the others
# can be left empty.

DEEPSEEK_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MOONSHOT_API_KEY=
DASHSCOPE_API_KEY=
GEMINI_API_KEY=
